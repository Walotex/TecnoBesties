#include <stdio.h>
#include "Odd.h"
#include "max.h"
#include "min.h"
#include "find.h"

int main (){
    int array[] = {3, 1 ,10, 14};

    printf("Array values: {%d, %d, %d, %d}\n", array [0], array [1], array [2], array [3]);

     scanf("%d", find);
     printf("Number found", find);

    return 0;
}